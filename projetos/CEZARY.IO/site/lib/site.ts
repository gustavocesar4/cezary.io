/**
 * URL final da CEZARY.IO. O domínio ainda não está registrado/apontado
 * (pendência de Fase 6) — usar este valor já agora é seguro porque é só
 * código; não depende de nenhuma conta externa. Atualizar aqui se o
 * domínio definitivo mudar.
 */
export const SITE_URL = "https://cezary.io";
